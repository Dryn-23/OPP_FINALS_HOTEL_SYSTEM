<Page x:Class="OOP_FINALS.BookingsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:sys="clr-namespace:System;assembly=mscorlib"
      Title="Hotel Admin - Bookings Management">

    <Page.Resources>
        <!-- Modern Color Palette -->
        <SolidColorBrush x:Key="PrimaryColor" Color="#6366F1"/>
        <SolidColorBrush x:Key="PrimaryDark" Color="#654321"/>
        <SolidColorBrush x:Key="SecondaryColor" Color="#10B981"/>
        <SolidColorBrush x:Key="DangerColor" Color="#EF4444"/>
        <SolidColorBrush x:Key="WarningColor" Color="#F59E0B"/>
        <SolidColorBrush x:Key="SurfaceColor" Color="#F8FAFC"/>
        <SolidColorBrush x:Key="CardBackground" Color="White"/>
        <SolidColorBrush x:Key="BorderColor" Color="#E2E8F0"/>
        <SolidColorBrush x:Key="TextPrimary" Color="#1E293B"/>
        <SolidColorBrush x:Key="TextSecondary" Color="#64748B"/>

        <!-- Shadows -->
        <DropShadowEffect x:Key="CardShadow" Color="#00000015" Direction="270" ShadowDepth="0" BlurRadius="20" Opacity="0.3"/>
        <DropShadowEffect x:Key="ButtonShadow" Color="#00000010" Direction="270" ShadowDepth="0" BlurRadius="8" Opacity="0.4"/>

        <!-- Status Badge Styles -->
        <Style x:Key="StatusBadgeStyle" TargetType="Border">
            <Setter Property="CornerRadius" Value="20"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="MinWidth" Value="80"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
        </Style>

        <!-- Right Aligned Text -->
        <Style x:Key="RightAlignedTextBlockStyle" TargetType="TextBlock">
            <Setter Property="HorizontalAlignment" Value="Right"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>

        <!-- DataGrid Cell Style -->
        <Style x:Key="ModernDataGridCellStyle" TargetType="DataGridCell">
            <Setter Property="Padding" Value="16,16"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#EFF6FF"/>
                </Trigger>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#F1F5F9"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- DataGrid Header Style -->
        <Style x:Key="ModernDataGridHeaderStyle" TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="{StaticResource SurfaceColor}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="16,20"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#E2E8F0"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Primary Button Style -->
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource PrimaryDark}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="16,12"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Effect" Value="{StaticResource ButtonShadow}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="12">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{StaticResource PrimaryDark}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#654321"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Success Button Style -->
        <Style x:Key="SuccessButton" TargetType="Button" BasedOn="{StaticResource ModernButton}">
            <Setter Property="Background" Value="{StaticResource SecondaryColor}"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#059669"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Danger Button Style -->
        <Style x:Key="DangerButton" TargetType="Button" BasedOn="{StaticResource ModernButton}">
            <Setter Property="Background" Value="{StaticResource DangerColor}"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#DC2626"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Modern TextBox Style -->
        <!-- FIXED Modern TextBox Style -->
        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="{StaticResource CardBackground}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="16,12"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="Border" 
                        Background="{TemplateBinding Background}" 
                        BorderBrush="{TemplateBinding BorderBrush}" 
                        BorderThickness="{TemplateBinding BorderThickness}" 
                        CornerRadius="12">
                            <!-- FIXED: PART_ContentHost is EMPTY - no content allowed! -->
                            <ScrollViewer x:Name="PART_ContentHost" 
                                  Focusable="False" 
                                  HorizontalScrollBarVisibility="Hidden" 
                                  VerticalScrollBarVisibility="Hidden"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="Border" Property="BorderBrush" Value="{StaticResource PrimaryColor}"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="BorderBrush" Value="{StaticResource PrimaryColor}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Modern ComboBox Style -->
        <Style x:Key="ModernComboBox" TargetType="ComboBox">
            <Setter Property="Background" Value="{StaticResource CardBackground}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <Border Background="{TemplateBinding Background}" 
                                    BorderBrush="{TemplateBinding BorderBrush}" 
                                    BorderThickness="{TemplateBinding BorderThickness}" 
                                    CornerRadius="12">
                                <Grid>
                                    <ContentPresenter x:Name="ContentSite" Margin="12,0"/>
                                    <Path x:Name="DropDownArrow" Data="M7,10L13,16L19,10" 
                                          Fill="{StaticResource TextSecondary}" 
                                          HorizontalAlignment="Right" 
                                          VerticalAlignment="Center" 
                                          Margin="0,0,12,0" 
                                          Width="12" Height="12"/>
                                </Grid>
                            </Border>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="BorderBrush" Value="{StaticResource PrimaryDark}"/>
                            </Trigger>
                            <Trigger Property="IsDropDownOpen" Value="True">
                                <Setter Property="BorderBrush" Value="{StaticResource PrimaryDark}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
    </Page.Resources>

    <Grid Margin="32">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Admin Header with Stats -->
        <StackPanel Grid.Row="0" Margin="0,0,0,32">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Grid.Column="0">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="Hotel Admin - Bookings Management" 
                               FontSize="32" 
                               FontWeight="Bold" 
                               Foreground="{StaticResource PrimaryDark}"/>
                    <StackPanel Orientation="Horizontal" Margin="24,0,0,0" VerticalAlignment="Center">
                        <Ellipse Width="10" Height="10" Fill="{StaticResource SecondaryColor}" Margin="0,0,12,0"/>
                        <TextBlock x:Name="txtBookingCount" 
                                   Text="0 Active" 
                                   FontSize="18" 
                                   FontWeight="SemiBold" 
                                   Foreground="{StaticResource TextPrimary}"/>
                    </StackPanel>
                </StackPanel>
                <Button x:Name="NewBookingButton" 
                        Content="➕ New Booking" 
                        Width="180" 
                        Height="48"
                        Style="{StaticResource ModernButton}"
                        Click="NewBookingButton_Click"/>
            </StackPanel>

            <!-- Quick Stats Cards -->
            <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,16,0,0" HorizontalAlignment="Stretch">
                <Border Background="{StaticResource CardBackground}" 
                        CornerRadius="16" 
                        Effect="{StaticResource CardShadow}" 
                        Padding="24" 
                        Margin="0,0,12,0" 
                        HorizontalAlignment="Stretch">
                    <StackPanel>
                        <TextBlock Text="Total Revenue" FontSize="28" FontWeight="Bold" Foreground="{StaticResource PrimaryDark}"/>
                        <TextBlock x:Name="txtTotalRevenue" Text="₱0.00" FontSize="36" FontWeight="Bold" Foreground="{StaticResource TextPrimary}"/>
                    </StackPanel>
                </Border>
                <Border Background="{StaticResource CardBackground}" 
                        CornerRadius="16" 
                        Effect="{StaticResource CardShadow}" 
                        Padding="24" 
                        Margin="12,0,12,0" 
                        HorizontalAlignment="Stretch">
                    <StackPanel>
                        <TextBlock Text="Occupancy" FontSize="28" FontWeight="Bold" Foreground="{StaticResource SecondaryColor}"/>
                        <TextBlock x:Name="txtOccupancy" Text="0%" FontSize="36" FontWeight="Bold" Foreground="{StaticResource TextPrimary}"/>
                    </StackPanel>
                </Border>
                <Border Background="{StaticResource CardBackground}" 
                        CornerRadius="16" 
                        Effect="{StaticResource CardShadow}" 
                        Padding="24" 
                        Margin="12,0,0,0" 
                        HorizontalAlignment="Stretch">
                    <StackPanel>
                        <TextBlock Text="Pending Payments" FontSize="28" FontWeight="Bold" Foreground="{StaticResource WarningColor}"/>
                        <TextBlock x:Name="txtPendingPayments" Text="0" FontSize="36" FontWeight="Bold" Foreground="{StaticResource TextPrimary}"/>
                    </StackPanel>
                </Border>
            </StackPanel>
        </StackPanel>

        <!-- Enhanced Filters -->
        <Border Grid.Row="2" Background="{StaticResource SurfaceColor}" 
                Padding="28" Margin="0,0,0,28" CornerRadius="20" Effect="{StaticResource CardShadow}">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <!-- Room Type Filter -->
                <ComboBox x:Name="RoomTypeFilter" 
                          Width="160" 
                          Margin="0,0,16,0" 
                          Style="{StaticResource ModernComboBox}"
                          SelectionChanged="FilterChanged">
                    <ComboBoxItem Content="All Rooms" IsSelected="True"/>
                    <ComboBoxItem Content="Standard"/>
                    <ComboBoxItem Content="Deluxe"/>
                    <ComboBoxItem Content="Suite"/>
                </ComboBox>

                <!-- Status Filter -->
                <ComboBox x:Name="StatusFilter" 
                          Width="140" 
                          Margin="0,0,16,0" 
                          Style="{StaticResource ModernComboBox}"
                          SelectionChanged="FilterChanged">
                    <ComboBoxItem Content="All Status" IsSelected="True"/>
                    <ComboBoxItem Content="Confirmed"/>
                    <ComboBoxItem Content="Checked In"/>
                    <ComboBoxItem Content="Checked Out"/>
                    <ComboBoxItem Content="Cancelled"/>
                </ComboBox>

                <!-- Date Range -->
                <DatePicker x:Name="FromDateFilter" Width="150" Margin="0,0,8,0" SelectedDateChanged="FilterChanged"/>
                <TextBlock Text="to" VerticalAlignment="Center" Margin="4,0" Foreground="{StaticResource TextSecondary}"/>
                <DatePicker x:Name="ToDateFilter" Width="150" Margin="8,0,20,0" SelectedDateChanged="FilterChanged"/>

                <!-- Guest Search -->
                <!-- Clean Search TextBox with Placeholder -->
                <!-- Guest Search -->
                <!-- Clean Search TextBox with Placeholder -->
                <TextBox x:Name="SearchTextBox" 
         Width="280" 
         Margin="0,0,16,0"
         TextChanged="SearchTextBox_TextChanged"
         GotFocus="SearchTextBox_GotFocus"
         LostFocus="SearchTextBox_LostFocus">
                    <TextBox.Style>
                        <Style TargetType="TextBox" BasedOn="{StaticResource ModernTextBox}">
                            <Style.Triggers>
                                <!-- Show placeholder text when empty and not focused -->
                                <MultiTrigger>
                                    <MultiTrigger.Conditions>
                                        <Condition Property="Text" Value=""/>
                                        <Condition Property="IsFocused" Value="False"/>
                                    </MultiTrigger.Conditions>
                                    <Setter Property="Foreground" Value="{StaticResource TextSecondary}"/>
                                </MultiTrigger>

                                <!-- Clear placeholder foreground when focused or has text -->
                                <MultiTrigger>
                                    <MultiTrigger.Conditions>
                                        <Condition Property="IsFocused" Value="True"/>
                                    </MultiTrigger.Conditions>
                                    <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
                                </MultiTrigger>

                                <MultiTrigger>
                                    <MultiTrigger.Conditions>
                                        <Condition Property="Text" Value="{x:Static sys:String.Empty}"/>
                                        <Condition Property="IsFocused" Value="False"/>
                                    </MultiTrigger.Conditions>
                                    <Setter Property="Foreground" Value="{StaticResource TextSecondary}"/>
                                </MultiTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBox.Style>
                </TextBox>

                <!-- Action Buttons -->
                <Button x:Name="RefreshButton" 
                        Content="🔄 Refresh" 
                        Width="120"
                        Click="RefreshButton_Click"
                        Style="{StaticResource ModernButton}"
                        Margin="0,0,8,0"/>

                <Button x:Name="ExportButton" 
                        Content="📊 Export" 
                        Width="120"
                        Click="ExportButton_Click"
                        Style="{StaticResource SuccessButton}"/>
            </StackPanel>
        </Border>

        <!-- Enhanced DataGrid with Admin Actions -->
        <Border Grid.Row="3" Background="{StaticResource CardBackground}" 
                BorderBrush="{StaticResource BorderColor}" 
                BorderThickness="1" 
                CornerRadius="20" 
                Effect="{StaticResource CardShadow}">
            <Grid>
                <DataGrid x:Name="dgBookings"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False"
                          IsReadOnly="True"
                          SelectionMode="Single"
                          GridLinesVisibility="None"
                          HeadersVisibility="Column"
                          Margin="24"
                          RowBackground="Transparent"
                          AlternatingRowBackground="{StaticResource SurfaceColor}"
                          CellStyle="{StaticResource ModernDataGridCellStyle}"
                          ColumnHeaderStyle="{StaticResource ModernDataGridHeaderStyle}"
                          SelectionChanged="dgBookings_SelectionChanged">
                    <DataGrid.Columns>
                        <!-- Checkbox Column -->
                        <DataGridTemplateColumn Width="40" CanUserResize="False">
                            <DataGridTemplateColumn.Header>
                                <CheckBox x:Name="SelectAllCheckbox" Click="SelectAllCheckbox_Click"/>
                            </DataGridTemplateColumn.Header>
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <CheckBox IsChecked="{Binding IsSelected}"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <DataGridTextColumn Header="Booking ID" Binding="{Binding BookingID}" Width="140" FontWeight="SemiBold"/>
                        <DataGridTextColumn Header="Guest Name" Binding="{Binding GuestName}" Width="200"/>
                        <DataGridTextColumn Header="Room" Binding="{Binding RoomNumber}" Width="100"/>
                        <DataGridTextColumn Header="Room Type" Binding="{Binding RoomType}" Width="120"/>
                        <DataGridTextColumn Header="Check In" Binding="{Binding CheckInDate, StringFormat={}{0:dd MMM yyyy}}" Width="130"/>
                        <DataGridTextColumn Header="Check Out" Binding="{Binding CheckOutDate, StringFormat={}{0:dd MMM yyyy}}" Width="130"/>
                        <DataGridTextColumn Header="Nights" Binding="{Binding NightsStay}" Width="80"/>
                        <DataGridTextColumn Header="Amount" Binding="{Binding TotalAmount, StringFormat=C}" Width="140">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="HorizontalAlignment" Value="Right"/>
                                    <Setter Property="FontWeight" Value="SemiBold"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                        
                        <!-- Status Column with Badge -->
                        <DataGridTemplateColumn Header="Status" Width="140">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Border Style="{StaticResource StatusBadgeStyle}">
                                        <TextBlock Text="{Binding Status}" FontWeight="SemiBold" HorizontalAlignment="Center">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding Status}" Value="Confirmed">
                                                            <Setter Property="Foreground" Value="White"/>
                                                            <Setter Property="Background">
                                                                <Setter.Value>
                                                                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                                                        <GradientStop Color="#3B82F6" Offset="0"/>
                                                                        <GradientStop Color="#1D4ED8" Offset="1"/>
                                                                    </LinearGradientBrush>
                                                                </Setter.Value>
                                                            </Setter>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding Status}" Value="Checked In">
                                                            <Setter Property="Foreground" Value="White"/>
                                                            <Setter Property="Background">
                                                                <Setter.Value>
                                                                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                                                        <GradientStop Color="#10B981" Offset="0"/>
                                                                        <GradientStop Color="#059669" Offset="1"/>
                                                                    </LinearGradientBrush>
                                                                </Setter.Value>
                                                            </Setter>
                                                        </DataTrigger>

                                                        <DataTrigger Binding="{Binding Status}" Value="Checked Out">
                                                            <Setter Property="Foreground" Value="White"/>
                                                            <Setter Property="Background">
                                                                <Setter.Value>
                                                                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                                                        <GradientStop Color="#6B7280" Offset="0"/>
                                                                        <GradientStop Color="#374151" Offset="1"/>
                                                                    </LinearGradientBrush>
                                                                </Setter.Value>
                                                            </Setter>
                                                        </DataTrigger>

                                                        <DataTrigger Binding="{Binding Status}" Value="Cancelled">
                                                            <Setter Property="Foreground" Value="White"/>
                                                            <Setter Property="Background">
                                                                <Setter.Value>
                                                                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                                                        <GradientStop Color="#EF4444" Offset="0"/>
                                                                        <GradientStop Color="#B91C1C" Offset="1"/>
                                                                    </LinearGradientBrush>
                                                                </Setter.Value>
                                                            </Setter>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </Border>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <!-- Actions Column -->
                        <DataGridTemplateColumn Header="Actions" Width="220">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                        <Button Content="✔ Check In"
                                                Margin="4,0"
                                                Padding="8,4"
                                                Style="{StaticResource SuccessButton}"
                                                Click="CheckIn_Click"/>

                                        <Button Content="↩ Check Out"
                                                Margin="4,0"
                                                Padding="8,4"
                                                Style="{StaticResource ModernButton}"
                                                Click="CheckOut_Click"/>

                                        <Button Content="✖ Cancel"
                                                Margin="4,0"
                                                Padding="8,4"
                                                Style="{StaticResource DangerButton}"
                                                Click="Cancel_Click"/>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>
    </Grid>
</Page>